import { Navigate } from 'react-router-dom'

const Account = () => {
  // User information
  // Change password
  // Order history
  // Logout

  return <Navigate to='/login' />
}

export default Account
